useDynLib(multiscale, .registration=TRUE)
export(compute_minimal_intervals,
       compute_quantiles,
       construct_grid,
       estimate_lrv,
       multiscale_test,
       plot_sizer_map,
       select_order)
importFrom(Rcpp, evalCpp)
importFrom('graphics', 'image')
importFrom('stats', 'quantile')
